# SwiftLint Configuration for FeaturePulse SDK

excluded:
  - .build
  - .swiftpm
  - Tests

identifier_name:
  excluded:
    - ok
    - id

nesting:
  type_level: 2

line_length:
  ignores_urls: true
