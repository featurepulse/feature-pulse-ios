# FeaturePulse iOS SDK - Cursor Rules

## 📖 Full Development Guide
See [DEVELOPMENT.md](./DEVELOPMENT.md) for complete development guidelines.

## ⚡ Quick Reference

### Before Every Commit
```bash
cd ios-sdk
swiftlint --fix  # Auto-fix style issues
swiftlint        # Verify no violations in FeaturePulse code
```
**Note**: Ignore violations in external dependencies (StableID) or generated files.

### Platform Support
- iOS 17.0+ | macOS 14.0+ | visionOS 1.0+
- Use `#if os(iOS)` for iOS-only APIs

### Distribution
- ❌ DO NOT push to public repo directly
- ✅ Test in private repo first
- ✅ Wait for approval before publishing new versions
